# Database Configuration
# These values work with the default docker-compose.yml configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=devfriend
DB_USER=devfriend
DB_PASSWORD=devfriend

# Encryption Key for secrets (Fernet key - 32 bytes base64 encoded)
# REQUIRED: This key is used to encrypt/decrypt sensitive data
# Generate a new one with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DEVFRIEND_ENCRYPTION_KEY=qRh6J2wkERU_4ImYtK_9WhcZPxMFEQP4o8pcTGV_Drk=

# Google OAuth Configuration (Optional - only needed for Gmail integration and Google login)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend URL (Optional - auto-detected if not set)
FRONTEND_URL=
# GitHub
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET
