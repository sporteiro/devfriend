# Database Configuration
# These values work with the default docker-compose.yml configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=devfriend
DB_USER=devfriend
DB_PASSWORD=devfriend

# Encryption key for secrets (generate a new one for production)
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DEVFRIEND_ENCRYPTION_KEY=qRh6J2wkERU_4ImYtK_9WhcZPxMFEQP4o8pcTGV_Drk=

# Frontend URL (used for OAuth redirects)
FRONTEND_URL=http://localhost:88

# OAuth Integrations (leave empty if not using)
# Gmail
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8888/auth/google/callback
# GitHub
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:8888/auth/github/callback
# Slack
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_REDIRECT_URI=http://localhost:8888/auth/slack/callback

# JWT Secret (change for production!)
JWT_SECRET_KEY=asdfjñsdkflj12341234
