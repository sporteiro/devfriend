services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: devfriend
      POSTGRES_USER: devfriend
      POSTGRES_PASSWORD: devfriend
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db_schema.sql:/docker-entrypoint-initdb.d/db_schema.sql:ro
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devfriend"]
      interval: 5s
      timeout: 5s
      retries: 5

  back:
    build:
      context: ./back
    ports:
      - "8888:8888"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: devfriend
      DB_USER: devfriend
      DB_PASSWORD: devfriend
    env_file:
      - ./back/.env
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    # auto-reload and hot reload:
    volumes:
      - ./back/src:/app/src
    command: uvicorn src.main:app --host 0.0.0.0 --port 8888 --reload --log-level debug

  front:
    build: ./front
    ports:
      - "88:88"
    volumes:
      - ./front:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: sh -c "mkdir -p /app/node_modules/.cache && chmod -R 777 /app/node_modules && npm run serve -- --port 88 --host 0.0.0.0"
    stdin_open: true
    tty: true
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
